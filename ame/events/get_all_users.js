exports.run = (io, socket, data, callback) => {
    // Get all users
    // const users = [];
    // io.sockets.sockets.map(u => users.push({username:u.username, colour:u.colour}));
    // Send back as response
    // callback(users);
    // console.log({username:users});
    callback([]);
};